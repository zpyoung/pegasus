// IdeationView re-exports the IdeaBoard as the main view.
// The route file imports IdeationView by name, so this export keeps routing unchanged.
export { IdeaBoard as IdeationView } from "./idea-board";
