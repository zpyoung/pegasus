/**
 * Centralized port configuration for Pegasus
 *
 * Re-exports from @pegasus/types for backward compatibility.
 * The canonical definition is in @pegasus/types to allow browser-safe imports.
 */

export { STATIC_PORT, SERVER_PORT, RESERVED_PORTS } from "@pegasus/types";
